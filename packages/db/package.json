{
  "name": "@the-guard/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "prisma generate --schema=./prisma/schema.prisma && node -e \"require('fs').rmSync('dist',{recursive:true,force:true});try{require('fs').unlinkSync('tsconfig.tsbuildinfo')}catch{}\" && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(db) add eslint config later\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
