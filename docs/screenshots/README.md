# Screenshots

Dark-mode captures at **1440px** width recommended for consistent typography.

## Checked-in (`s1`–`s4`)

| Theme | File | README reference |
|-------|------|------------------|
| Release overview (latest runs + verdict) | `s1.png` | Root README **Screenshots** |
| Regression detail (falling examples, traces) | `s2.png` | Same |
| History / timeline / trends | `s3.png` | Same |
| Hallucination evidence / deterministic traces | `s4.png` | Same |

## Optional additional filenames

If you add more exports for reviewers, these names match legacy README placeholders:

- `release-overview.png` — dashboard home / verdict panel  
- `regression-detail.png` — `/regressions/[id]` deep-dive  
- `hallucination-traces.png` — hallucination evidence panel  
- `benchmark-dashboard.png` — `/benchmarks` cost vs quality view  
- `timeline-view.png` — `/history` operational timeline  

Keep binary sizes reasonable; prefer PNG or WebP.
